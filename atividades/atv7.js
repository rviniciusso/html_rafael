let tarefas = [
    {nome: 'estudar python', 'id': 0, concluido: true},
    {nome: 'estudar css', 'id': 1, concluido: false},
    {nome: 'estudar js', 'id': 2, concluido: false}
]

const buscarprojeto = objetos.find(objetos => objetos.id === 2)
console.log(buscarobjeto)
const concluida = objetos.filter(objetos => objetos.concluida === true)
const buscarnomes = objetos.map(objetos => objetos.nome)
const removertarefa = (nome) => {
    
}
